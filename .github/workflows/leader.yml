name: blank

on:
  push:

jobs:
  biology:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        foundation: ["singular", "any", "chemist", "vivid", "ugly"]
        handy: ["preferentially", "challenge", "cite", "spoil"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar cn
